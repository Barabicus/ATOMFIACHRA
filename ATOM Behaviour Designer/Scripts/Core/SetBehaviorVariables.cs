using UnityEngine;
using System.Collections;
using BehaviorDesigner.Runtime;

public class SetBehaviorVariables : MonoBehaviour {

	// Use this for initialization
    private void Start()
    {
        BehaviorTree bt;
        
    }

    // Update is called once per frame
	void Update () {
	
	}
}
